{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["**/*"],
      "outputs": ["dist/**/*"],
      "outputMode": "new-only"
    }
  }
}
